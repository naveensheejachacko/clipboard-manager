"""Names shown to users and in .desktop files (searchable on app menus)."""

# What users see in the menu, tray, and window.
APP_DISPLAY_NAME = "Clipboard Manager"

# Short tagline for Software Center / .desktop Comment.
APP_SUMMARY = "Clipboard history — search, pin, and paste like Windows Win+V"

# Command installed on PATH (primary).
APP_COMMAND = "clipboard-manager"

# Old command name; kept as a symlink for existing shortcuts.
LEGACY_COMMAND = "winclip"

# Freedesktop keywords (app menu search).
APP_KEYWORDS = "clipboard;manager;history;copy;paste;win+v;clip;clipman;copyq"
